# Git
.git
.gitignore

# gstack / Claude 로컬 전용 폴더
.gstack
.claude
.context

# 환경변수 (절대 이미지에 포함되면 안 됨)
.env
.env.local
.env.*.local

# Node 의존성 (이미지 안에서 다시 설치)
node_modules
**/node_modules

# 빌드 아티팩트
**/dist
**/.next
**/coverage
**/.turbo

# 로그
**/*.log
**/logs

# 업로드 파일 (런타임에 볼륨으로 마운트)
apps/api/uploads

# 개발 전용
**/*.spec.ts
**/*.test.ts
**/test
docker-compose*.yml
*.md
